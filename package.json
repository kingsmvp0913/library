{
  "name": "library",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node scripts/start.js",
    "setup": "node scripts/setup.js",
    "test": "jest --runInBand",
    "test:quiet": "jest --runInBand --silent --noStackTrace --no-color"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "pg-mem": "^2.9.1",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/server/tests/**/*.test.js"],
    "setupFiles": ["<rootDir>/server/tests/setup-env.js"]
  }
}
